# 第三层：体验增强

> **目标**：在地基和技术深度完成后，添加缓存、限流、评估、跨模块联动等增强功能  
> **预计耗时**：2-3 天  
> **前置依赖**：[01-地基工程.md](./01-地基工程.md) 和 [02-技术深度.md](./02-技术深度.md) 完成

---

## 任务 3.1：语义缓存（降低 API 成本）

### 背景

DashScope API 按 Token 计费，相似问题重复调用会浪费成本。通过语义缓存，将历史优质问答向量化存储，新问题先检索缓存，命中则直接返回。

### 实施步骤

**新建文件**：`smart-ai-service/src/main/java/com/oceanverse/ai/cache/SemanticCacheService.java`

```java
package com.oceanverse.ai.cache;

import com.oceanverse.ai.mapper.QaHistoryMapper;
import com.oceanverse.pojo.entity.QaHistory;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.ai.embedding.EmbeddingModel;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.stereotype.Service;

import java.util.List;

@Slf4j
@Service
@RequiredArgsConstructor
public class SemanticCacheService {

    private static final String CACHE_KEY_PREFIX = "ai:cache:";
    private static final double SIMILARITY_THRESHOLD = 0.95;  // 相似度阈值

    private final EmbeddingModel embeddingModel;
    private final RedisTemplate<String, String> redisTemplate;
    private final QaHistoryMapper qaHistoryMapper;

    /**
     * 尝试从缓存中获取答案
     * @return 缓存的答案，未命中返回 null
     */
    public String getCachedAnswer(String question) {
        try {
            // 1. 将问题向量化
            float[] questionVector = embeddingModel.embed(question);
            
            // 2. 在 Redis 中查找相似问题
            // 这里简化实现：实际应使用 Redis Stack 的向量检索
            // 或者退化为精确匹配（对高频问题做 MD5 哈希）
            String cacheKey = CACHE_KEY_PREFIX + md5(question);
            String cached = redisTemplate.opsForValue().get(cacheKey);
            
            if (cached != null) {
                log.info("语义缓存命中: {}", question);
                return cached;
            }
            
        } catch (Exception e) {
            log.warn("语义缓存查询失败: {}", e.getMessage());
        }
        
        return null;
    }

    /**
     * 将高质量问答存入缓存
     */
    public void cacheAnswer(String question, String answer) {
        try {
            String cacheKey = CACHE_KEY_PREFIX + md5(question);
            redisTemplate.opsForValue().set(cacheKey, answer, 24, java.util.concurrent.TimeUnit.HOURS);
            log.info("已缓存问答: {}", question);
        } catch (Exception e) {
            log.warn("语义缓存写入失败: {}", e.getMessage());
        }
    }

    /**
     * 预热缓存：从数据库中加载高满意度问答
     */
    public void warmupCache() {
        List<QaHistory> highQualityAnswers = qaHistoryMapper.selectList(
            new com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper<QaHistory>()
                .eq(QaHistory::getFeedback, 1)  // 只缓存满意的答案
                .isNotNull(QaHistory::getAnswerText)
                .last("LIMIT 100")
        );
        
        for (QaHistory history : highQualityAnswers) {
            cacheAnswer(history.getQuestionText(), history.getAnswerText());
        }
        
        log.info("缓存预热完成，共加载 {} 条高质量问答", highQualityAnswers.size());
    }

    private String md5(String input) {
        return org.springframework.util.DigestUtils.md5DigestAsHex(input.getBytes());
    }
}
```

**修改 AiServiceImpl.chatStream()**：

```java
@Override
public void chatStream(ChatDTO dto, HttpServletResponse response) {
    // 1. 尝试语义缓存
    String cachedAnswer = semanticCacheService.getCachedAnswer(dto.getQuestion());
    if (cachedAnswer != null) {
        // 直接返回缓存答案（模拟流式）
        streamCachedAnswer(cachedAnswer, response);
        return;
    }

    // 2. 未命中，走正常 RAG + 模型调用流程
    // ... 原有逻辑 ...

    // 3. 流结束后，缓存高质量回答
    if (history.getFeedback() == null || history.getFeedback() == 1) {
        semanticCacheService.cacheAnswer(dto.getQuestion(), fullAnswer.toString());
    }
}

private void streamCachedAnswer(String answer, HttpServletResponse response) {
    try {
        PrintWriter writer = response.getWriter();
        for (String chunk : answer.split("(?<=\\G.{10})")) {  // 每 10 字符一个 chunk
            writer.write("data: " + chunk + "\n\n");
            writer.flush();
            Thread.sleep(30);  // 模拟流式效果
        }
        writer.write("data: [DONE][CACHED]\n\n");
        writer.flush();
    } catch (Exception e) {
        log.error("返回缓存答案失败", e);
    }
}
```

### 关键技术点

1. **MD5 哈希缓存**：简化实现，对完全相同的问题直接命中
2. **语义相似度**：进阶实现可用 Embedding 向量 + Redis 向量检索，匹配相似问题
3. **缓存预热**：应用启动时从数据库加载高满意度问答，避免冷启动
4. **TTL 管理**：缓存 24 小时过期，保证答案时效性

### 验收标准

- [ ] 同一个问题第二次提问时，响应明显加快（< 500ms）
- [ ] SSE 流中包含 `[CACHED]` 标记，前端可识别
- [ ] 应用启动日志显示"缓存预热完成，共加载 N 条高质量问答"

---

## 任务 3.2：API 调用限流（成本控制）

### 背景

AI API 按调用量计费，需要限制单用户的调用频率，防止滥用或意外刷量。

### 实施步骤

**新建文件**：`smart-ai-service/src/main/java/com/oceanverse/ai/ratelimit/AiRateLimiter.java`

```java
package com.oceanverse.ai.ratelimit;

import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.stereotype.Component;

import java.time.Duration;
import java.time.LocalDate;
import java.time.format.DateTimeFormatter;

@Slf4j
@Component
@RequiredArgsConstructor
public class AiRateLimiter {

    private static final String RATE_LIMIT_KEY_PREFIX = "ai:ratelimit:";
    private static final int DAILY_CHAT_LIMIT = 50;       // 普通用户每日问答上限
    private static final int DAILY_RECOGNITION_LIMIT = 10; // 普通用户每日识别上限

    private final RedisTemplate<String, String> redisTemplate;

    /**
     * 检查用户是否超过调用限制
     * @return true = 允许调用，false = 已超限
     */
    public boolean isAllowed(Long userId, String actionType) {
        String dateStr = LocalDate.now().format(DateTimeFormatter.ISO_LOCAL_DATE);
        String key = RATE_LIMIT_KEY_PREFIX + userId + ":" + actionType + ":" + dateStr;
        
        String countStr = redisTemplate.opsForValue().get(key);
        int count = countStr != null ? Integer.parseInt(countStr) : 0;
        
        int limit = "chat".equals(actionType) ? DAILY_CHAT_LIMIT : DAILY_RECOGNITION_LIMIT;
        
        if (count >= limit) {
            log.warn("用户 {} 已超过 {} 调用限制 ({}/{})", userId, actionType, count, limit);
            return false;
        }
        
        return true;
    }

    /**
     * 记录一次调用
     */
    public void recordCall(Long userId, String actionType) {
        String dateStr = LocalDate.now().format(DateTimeFormatter.ISO_LOCAL_DATE);
        String key = RATE_LIMIT_KEY_PREFIX + userId + ":" + actionType + ":" + dateStr;
        
        redisTemplate.opsForValue().increment(key);
        redisTemplate.expire(key, Duration.ofDays(2));  // 2 天后自动过期
    }

    /**
     * 获取剩余可用次数
     */
    public int getRemainingQuota(Long userId, String actionType) {
        String dateStr = LocalDate.now().format(DateTimeFormatter.ISO_LOCAL_DATE);
        String key = RATE_LIMIT_KEY_PREFIX + userId + ":" + actionType + ":" + dateStr;
        
        String countStr = redisTemplate.opsForValue().get(key);
        int count = countStr != null ? Integer.parseInt(countStr) : 0;
        
        int limit = "chat".equals(actionType) ? DAILY_CHAT_LIMIT : DAILY_RECOGNITION_LIMIT;
        return Math.max(0, limit - count);
    }
}
```

**修改 AiController**：

```java
@PostMapping("/recognize")
public Result<Object> recognize(@RequestParam("file") MultipartFile file,
                                 @RequestParam(value = "latitude", required = false) Double latitude,
                                 @RequestParam(value = "longitude", required = false) Double longitude,
                                 @AuthenticationPrincipal Long userId) {
    // 限流检查
    if (!aiRateLimiter.isAllowed(userId, "recognition")) {
        return Result.error("今日识别次数已用完，请明天再试");
    }
    
    aiRateLimiter.recordCall(userId, "recognition");
    return Result.success(aiService.recognizeImage(file, latitude, longitude));
}

@PostMapping("/chat")
public void chat(@RequestBody ChatDTO dto, HttpServletResponse response,
                 @AuthenticationPrincipal Long userId) {
    // 限流检查
    if (!aiRateLimiter.isAllowed(userId, "chat")) {
        response.setContentType("text/event-stream;charset=UTF-8");
        try {
            PrintWriter writer = response.getWriter();
            writer.write("data: [ERROR] 今日问答次数已用完，请明天再试\n\n");
            writer.write("data: [DONE]\n\n");
            writer.flush();
        } catch (Exception e) {
            log.error("返回限流提示失败", e);
        }
        return;
    }
    
    aiRateLimiter.recordCall(userId, "chat");
    response.setContentType("text/event-stream;charset=UTF-8");
    response.setCharacterEncoding("UTF-8");
    aiService.chatStream(dto, response);
}
```

**新增接口：查询剩余配额**：

```java
@GetMapping("/quota")
public Result<Object> getQuota(@AuthenticationPrincipal Long userId) {
    Map<String, Object> quota = new HashMap<>();
    quota.put("chatRemaining", aiRateLimiter.getRemainingQuota(userId, "chat"));
    quota.put("recognitionRemaining", aiRateLimiter.getRemainingQuota(userId, "recognition"));
    return Result.success(quota);
}
```

### 关键技术点

1. **滑动窗口限流**：基于 Redis INCR + EXPIRE，按天统计调用次数
2. **用户等级扩展**：后续可根据用户角色（普通/会员/管理员）设置不同配额
3. **优雅降级**：超限时返回友好提示，不抛 429 错误
4. **配额查询**：前端可展示"今日剩余 X 次"提示

### 验收标准

- [ ] 普通用户第 51 次问答时，收到"今日次数已用完"提示
- [ ] `GET /api/ai/quota` 返回正确的剩余次数
- [ ] 每日 0 点后，计数自动重置
- [ ] 限流检查在模型调用之前执行，避免浪费 API 配额

---

## 任务 3.3：AI 效果评估接口

### 背景

需要数据驱动地评估 AI 模块的效果：满意度如何？哪些问题答得好？识别准确率多少？

### 实施步骤

**新建文件**：`smart-ai-service/src/main/java/com/oceanverse/ai/eval/AiEvalService.java`

```java
package com.oceanverse.ai.eval;

import com.oceanverse.ai.mapper.ImageRecognitionMapper;
import com.oceanverse.ai.mapper.QaHistoryMapper;
import com.oceanverse.pojo.entity.ImageRecognition;
import com.oceanverse.pojo.entity.QaHistory;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;

import java.math.BigDecimal;
import java.math.RoundingMode;
import java.util.*;

@Service
@RequiredArgsConstructor
public class AiEvalService {

    private final QaHistoryMapper qaHistoryMapper;
    private final ImageRecognitionMapper recognitionMapper;

    /**
     * 获取 AI 效果评估统计
     */
    public Map<String, Object> getEvalStats() {
        Map<String, Object> stats = new HashMap<>();

        // ===== 问答统计 =====
        List<QaHistory> allQa = qaHistoryMapper.selectList(null);
        long totalQa = allQa.size();
        long satisfiedQa = allQa.stream().filter(q -> q.getFeedback() != null && q.getFeedback() == 1).count();
        long unsatisfiedQa = allQa.stream().filter(q -> q.getFeedback() != null && q.getFeedback() == 0).count();
        
        stats.put("qaTotal", totalQa);
        stats.put("qaSatisfied", satisfiedQa);
        stats.put("qaUnsatisfied", unsatisfiedQa);
        stats.put("qaSatisfactionRate", totalQa > 0 
            ? BigDecimal.valueOf(satisfiedQa * 100.0 / totalQa).setScale(1, RoundingMode.HALF_UP) 
            : BigDecimal.ZERO);
        
        // 平均响应时间
        double avgProcessingTime = allQa.stream()
            .filter(q -> q.getProcessingTimeMs() != null)
            .mapToInt(QaHistory::getProcessingTimeMs)
            .average()
            .orElse(0);
        stats.put("qaAvgProcessingTimeMs", (int) avgProcessingTime);

        // 热门问题 Top 10（按提问次数）
        Map<String, Long> questionFrequency = allQa.stream()
            .filter(q -> q.getQuestionText() != null)
            .collect(java.util.stream.Collectors.groupingBy(
                QaHistory::getQuestionText, 
                java.util.stream.Collectors.counting()
            ));
        
        List<Map.Entry<String, Long>> topQuestions = questionFrequency.entrySet().stream()
            .sorted(Map.Entry.<String, Long>comparingByValue().reversed())
            .limit(10)
            .toList();
        
        stats.put("qaTopQuestions", topQuestions.stream()
            .map(e -> Map.of("question", e.getKey(), "count", e.getValue()))
            .toList());

        // 问题类型分布
        Map<String, Long> typeDistribution = allQa.stream()
            .filter(q -> q.getQuestionType() != null)
            .collect(java.util.stream.Collectors.groupingBy(
                QaHistory::getQuestionType, 
                java.util.stream.Collectors.counting()
            ));
        stats.put("qaTypeDistribution", typeDistribution);

        // ===== 识别统计 =====
        List<ImageRecognition> allRecognitions = recognitionMapper.selectList(null);
        long totalRecognitions = allRecognitions.size();
        long verifiedRecognitions = allRecognitions.stream()
            .filter(r -> "VERIFIED".equals(r.getVerificationStatus()))
            .count();
        
        stats.put("recognitionTotal", totalRecognitions);
        stats.put("recognitionVerified", verifiedRecognitions);
        stats.put("recognitionAccuracyRate", totalRecognitions > 0 
            ? BigDecimal.valueOf(verifiedRecognitions * 100.0 / totalRecognitions).setScale(1, RoundingMode.HALF_UP) 
            : BigDecimal.ZERO);

        // 平均识别耗时
        double avgRecognitionTime = allRecognitions.stream()
            .filter(r -> r.getProcessingTimeMs() != null)
            .mapToInt(ImageRecognition::getProcessingTimeMs)
            .average()
            .orElse(0);
        stats.put("recognitionAvgProcessingTimeMs", (int) avgRecognitionTime);

        // 高置信度识别占比
        long highConfidence = allRecognitions.stream()
            .filter(r -> r.getConfidenceScore() != null && r.getConfidenceScore().compareTo(new BigDecimal("0.8")) >= 0)
            .count();
        stats.put("recognitionHighConfidenceRate", totalRecognitions > 0 
            ? BigDecimal.valueOf(highConfidence * 100.0 / totalRecognitions).setScale(1, RoundingMode.HALF_UP) 
            : BigDecimal.ZERO);

        return stats;
    }
}
```

**新增 Controller 接口**：

```java
@GetMapping("/eval/stats")
public Result<Object> getEvalStats() {
    return Result.success(aiEvalService.getEvalStats());
}
```

### 关键技术点

1. **满意度指标**：基于用户反馈计算满意率
2. **响应时间统计**：监控 AI 性能
3. **热门问题分析**：发现用户最关心的话题
4. **识别准确率**：通过 VERIFIED 状态评估识别质量

### 验收标准

- [ ] `GET /api/ai/eval/stats` 返回完整的统计数据
- [ ] 满意度计算正确（满意数 / 总反馈数）
- [ ] 热门问题 Top 10 按频率降序排列
- [ ] 识别准确率基于 VERIFIED 状态计算

---

## 任务 3.4：识别结果关联物种卡片

### 背景

当前图像识别只返回物种名称和置信度。增强为：识别后自动关联 `species` 表，返回完整的物种信息卡片。

### 实施步骤

**修改 AiServiceImpl.recognizeImage()**：

```java
@Override
public Object recognizeImage(MultipartFile file, Double latitude, Double longitude) {
    // ... 原有识别逻辑 ...

    // 识别成功后，关联 species 表
    if (record.getPredictedSpeciesName() != null && !"识别失败".equals(record.getPredictedSpeciesName())) {
        Species species = speciesMapper.selectOne(
            new LambdaQueryWrapper<Species>()
                .eq(Species::getCommonName, record.getPredictedSpeciesName())
                .or()
                .eq(Species::getScientificName, record.getPredictedSpeciesName())
                .last("LIMIT 1")
        );
        
        if (species != null) {
            record.setPredictedSpeciesId(species.getId());
            recognitionMapper.updateById(record);
            
            // 构造物种卡片数据
            Map<String, Object> speciesCard = new HashMap<>();
            speciesCard.put("id", species.getId());
            speciesCard.put("commonName", species.getCommonName());
            speciesCard.put("scientificName", species.getScientificName());
            speciesCard.put("conservationStatus", species.getConservationStatus());
            speciesCard.put("distribution", species.getDistribution());
            speciesCard.put("description", species.getDescription());
            speciesCard.put("imageUrl", species.getImageUrl());
            
            // 返回识别记录 + 物种卡片
            Map<String, Object> result = new HashMap<>();
            result.put("recognition", record);
            result.put("speciesCard", speciesCard);
            return result;
        }
    }

    return record;
}
```

### 关键技术点

1. **物种匹配**：按中文名或学名模糊匹配 species 表
2. **数据聚合**：将识别记录和物种信息组合返回
3. **前端联动**：前端可基于 `speciesCard` 渲染物种详情卡片

### 验收标准

- [ ] 识别出"绿海龟"后，返回结果中包含绿海龟的完整物种信息
- [ ] 物种匹配失败时，仍正常返回识别记录（不报错）
- [ ] 前端能正确渲染物种卡片（物种图片、分布、保护等级等）

---

## 任务 3.5：高置信度识别建议创建观测记录

### 背景

当识别置信度 > 0.8 时，说明 AI 对识别结果很有把握。此时可以提示用户"是否将此发现记录为一次生态观测？"——打通 `eco-observation-manager` 模块。

### 实施步骤

**修改 AiServiceImpl.recognizeImage()**：

```java
// 在返回结果中添加建议标记
if (record.getConfidenceScore() != null 
    && record.getConfidenceScore().compareTo(new BigDecimal("0.8")) >= 0
    && latitude != null && longitude != null) {
    
    result.put("suggestObservation", true);
    result.put("observationData", Map.of(
        "speciesId", record.getPredictedSpeciesId(),
        "speciesName", record.getPredictedSpeciesName(),
        "latitude", latitude,
        "longitude", longitude,
        "observationTime", LocalDateTime.now(),
        "confidence", record.getConfidenceScore()
    ));
}
```

**前端 Chat.vue / Recognize.vue**：

```javascript
if (response.suggestObservation) {
  ElMessageBox.confirm(
    `AI 以 ${(response.recognition.confidenceScore * 100).toFixed(0)}% 的置信度识别出 ${response.recognition.predictedSpeciesName}，是否记录为一次生态观测？`,
    '发现记录',
    { confirmButtonText: '记录观测', cancelButtonText: '不了' }
  ).then(() => {
    // 调用 eco-observation API 创建观测记录
    createObservation(response.observationData);
  });
}
```

### 关键技术点

1. **跨模块联动**：AI 模块 → 生态观测模块的数据流
2. **用户引导**：在合适的时机提示用户创建观测记录
3. **数据预填**：将识别结果自动填入观测表单，减少用户操作

### 验收标准

- [ ] 高置信度识别后，前端弹出"是否记录观测"提示
- [ ] 点击"记录观测"后，成功创建一条生态观测记录
- [ ] 观测记录的物种 ID、坐标、时间与识别结果一致
